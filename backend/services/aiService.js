// Abstracted AI service layer
exports.fixQuestion = async (questionText, currentLevel, targetLevel) => {
  // Call OpenAI/Gemini/Vertex API here
  // For now, stubbed response
  return `Updated (${targetLevel}): ${questionText}`;
};